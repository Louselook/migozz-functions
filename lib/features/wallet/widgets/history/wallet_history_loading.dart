import 'package:flutter/material.dart';

class WalletHistoryLoading extends StatelessWidget {
  const WalletHistoryLoading({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return(
      Text("Loading...")
    );
  }
}